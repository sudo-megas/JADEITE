# Source artefacts — real financial data in legible form.
#
# XJADEITE §18.2: the owner's private reference material for the manual typing
# sessions, kept on the archive HDD, never parsed by JADEITE and never opened by
# any tool acting on its behalf. These patterns are belt and braces — the files
# do not belong in the working tree at all.
JADEITorigin.xlsx
Altın_Eğrisi.pptx
*.xlsx
*.pptx

# Vault files, should one ever be created inside the working tree
*.db
*.db-wal
*.db-shm
*.keys
*.jbk

# Dependencies and build output
node_modules/
out/
dist/
release/
.vite/

# Editor and OS noise
.DS_Store
*.swp
.idea/
.vscode/
.claude/worktrees/
.claude/settings.local.json

# Test artefacts
coverage/
test-results/
playwright-report/
.tmp/
